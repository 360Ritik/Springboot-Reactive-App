server:
  port: 8088


spring:
  profiles:
    active: native

  application:
     name: CONFIG-SERVER
  cloud:
    config:
      server:
        git:
          uri: https://github.com/EtienneBel/spring-config-server
          clone-on-start: true
